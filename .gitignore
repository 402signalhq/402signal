# Secrets and credentials — never commit
*.env
.base.env
.algo.env
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*.pfx
id_rsa
id_rsa.*
id_ed25519
id_ed25519.*
credentials.json
**/credentials*
**/secrets/
*.secret
fly.toml.local

# Wallets / payer material (publish never includes these)
*wallet*.json
*payer*.json
*mnemonic*
*private*key*

# Sibling projects — not this repo
analogpair/
grantcomps/
402signal-pay/

# Python
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
.env/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# OS / editor
.DS_Store
.idea/
.vscode/
*.swp
*~

# Build-time asset fingerprint (written in the image, not authored)
.asset-version

# Fly local
.fly/

# Probe history (default lives in /tmp; never commit local DBs)
*.sqlite
*.sqlite-wal
*.sqlite-shm

# Development dependencies and build outputs
**/node_modules/
integration/mcp/.cache/
