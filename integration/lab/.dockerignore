# Local Dockerfile context (integration/lab). Fly publishes use
# Dockerfile.fly with integration/.dockerignore.
node_modules
dist
reports
*.sqlite*
work
.git

**/node_modules
**/dist
**/*.sqlite*
solana-session-contracts/test
