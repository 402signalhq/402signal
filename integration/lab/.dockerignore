# Used only if someone builds with context integration/lab. The Fly image
# uses integration/ as context and integration/.dockerignore.
node_modules
dist
reports
*.sqlite*
work
.git

**/node_modules
**/dist
**/*.sqlite*
solana-session-contracts/test
