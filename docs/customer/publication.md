# Publish the customer guidance without changing the protocol

This revision changes website/documentation copy and the source registry description. It does not publish a new registry version, enable hosted profiles, change transport URLs or alter MCP inputs. Registry versions may be immutable; use the repository's established release procedure for a valid metadata revision. Confirm downstream listing refresh independently.

The customer-facing paths are the website task guides, this Markdown index, the optional skill and the offline runner. The HTML and skill should describe the same supported tasks. Existing MCP tools still select between preview, validate and route; seller testing does not introduce an arbitrary-URL probing tool.

Inspect deployed tools/list, the installable adapter and each directory's actual representation after release. A changed README is not proof that Glama, MCP Registry, Context7 or a search index has refreshed. Do not invent adoption statistics or buy artificial traffic for ranking.

Keep service-fee acceptance separate from the merchant payment protocols the verifier inspects. Do not add marketing fields to closed payment objects or signed receipts. Source publication is not permission to spend.

The public source description is intentionally task-first. The fuller customer description is: Check a supported paid API offer against buyer rules, verify it locally before signing, and retain the original evidence. Qualifying hosted observations cost $0.003 USDC; completed no-match checks are free. Seller payment remains with the buyer.
